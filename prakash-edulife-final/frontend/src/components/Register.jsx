import React,{useState} from 'react'; import axios from 'axios'; import { useNavigate } from 'react-router-dom';
export default function Register(){ const nav=useNavigate(); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [msg,setMsg]=useState('');
  const submit=async e=>{ e.preventDefault(); try{ await axios.post(import.meta.env.VITE_API_URL+'/auth/register',{name,email,password}); setMsg('Registered! Redirecting...'); setTimeout(()=>nav('/'),1200); }catch(err){ setMsg(err.response?.data?.message||'Failed'); } };
  return (<div className='card'><div className='title'>Create your account</div><form onSubmit={submit}><input placeholder='Full name' value={name} onChange={e=>setName(e.target.value)} /><input placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)} /><input type='password' placeholder='Password' value={password} onChange={e=>setPassword(e.target.value)} /><button>Register</button></form>{msg && <div style={{marginTop:8}}>{msg}</div>}<a className='link' href='/'>Back to Login</a></div>); }
