# Prakash EduLife

Full 3-tier Student Management LMS (React + Node + PostgreSQL)

## Quickstart (Docker Compose)
1. Copy backend/.env.example -> backend/.env and edit if needed
2. Run: `docker compose up --build -d`
3. Frontend: http://localhost | Backend: http://localhost:5000
